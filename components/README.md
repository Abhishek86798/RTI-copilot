# components

Frontend-owned. Page-level and shared UI components. `components/ui/` holds shadcn/ui primitives (generated via `npx shadcn add <component>`) — extend rather than hand-edit those unless customizing a primitive intentionally.
