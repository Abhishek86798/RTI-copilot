# Neon Postgres (via Vercel Marketplace) — Phase 0/4
DATABASE_URL=

# Clerk — Phase 0/4
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Resend — Phase 4
RESEND_API_KEY=

# Vercel AI Gateway — Phase 1 (uses Vercel account's built-in $5/mo credit; no key needed when deployed on Vercel, set for local dev only if testing outside Vercel)
AI_GATEWAY_API_KEY=
