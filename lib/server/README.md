# lib/server

Backend-owned code: database access (Neon/Postgres), Clerk server helpers, Resend email sending, the authority-routing/legal-drafting logic that calls the AI Gateway, and Vercel Cron job handlers.

Pairs with API routes in [app/api/](../../app/api/).
