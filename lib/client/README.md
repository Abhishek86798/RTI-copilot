# lib/client

Frontend-owned code: localStorage/guest-mode state helpers, form validation, client-side formatting, and hooks used by pages and components.

`lib/utils.ts` (one level up) stays put — it's shadcn/ui's expected `cn()` helper location and its generated components import it from there directly.
