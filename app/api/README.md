# app/api

Backend-owned. Route handlers for authority routing, draft generation, filing/tracking, and the Cron-triggered appeal sweep — one subfolder per endpoint, e.g. `app/api/route-authority/route.ts`.
